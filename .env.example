# Optional: direct Printr attribution for Printr-native Solana tokens.
# Without this, BPB Lite falls back to public pair metadata only.
PRINTR_BEARER_TOKEN=your_printr_preview_bearer_token_here

# Optional: if you want to keep the public scanner aligned with the BPB naming.
BPB_PRINTR_BEARER_TOKEN=
